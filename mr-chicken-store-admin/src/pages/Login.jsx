import{useState}from"react";import{LockKeyhole,Mail,ArrowRight,Store}from"lucide-react";import{useAuth}from"../context/AuthContext";export default function Login(){const{signIn,supabaseConfigured}=useAuth();const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setError("");setBusy(true);const{error}=await signIn(email,password);if(error)setError(error.message);setBusy(false)};return <main className="login-page"><div className="login-card"><div className="login-logo"><div className="logo-mark large">MC</div><div className="counter-tag"><Store size={15}/>STORE ADMIN</div></div><h1>Welcome, team.</h1><p className="login-copy">Keep orders moving without slowing the kitchen down.</p>{!supabaseConfigured&&<div className="demo-note"><b>Preview mode</b><br/>Add Supabase keys to <code>.env</code> for real staff login and live data.</div>}<form onSubmit={submit}><label>Email<div className="input-wrap"><Mail size={18}/><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="staff@mrchicken.in"/></div></label><label>Password<div className="input-wrap"><LockKeyhole size={18}/><input required type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></div></label>{error&&<p className="error-text">{error}</p>}<button className="btn primary login-btn" disabled={busy}>{busy?"Signing in…":"Sign in"}<ArrowRight/></button></form></div><div className="login-art"><div className="fried-icon">🍗</div><h2>Orders in.<br/>Chicken out.</h2><p>Real-time counter operations for busy Mr. Chicken stores.</p></div></main>}