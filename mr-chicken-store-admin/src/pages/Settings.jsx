import{Store,LogOut,ShieldCheck}from"lucide-react";import{useAuth}from"../context/AuthContext";export default function Settings(){const{session,signOut}=useAuth();const name=session?.user?.user_metadata?.full_name||session?.user?.email?.split("@")[0]||"Store Staff";return <div className="simple-page"><div className="page-toolbar"><div><h2>Profile & Settings</h2><p>Basic staff information for this store device.</p></div></div><div className="settings-card"><div className="profile-avatar">{name[0].toUpperCase()}</div><div className="profile-info"><span>Staff name</span><h3>{name}</h3><p>{session?.user?.email||"Preview staff account"}</p></div></div><div className="settings-grid"><div className="info-card"><Store/><div><span>Store</span><strong>Main Store</strong><small>Mr. Chicken</small></div></div><div className="info-card"><ShieldCheck/><div><span>Access</span><strong>Store Admin</strong><small>Orders + availability</small></div></div></div><button className="btn danger-outline logout-big" onClick={signOut}><LogOut/>Log out from this device</button></div>}